# MASTER PROMPT — Personal Portfolio Website (ณันธภัทร์ ลิ่มเจริญ)

> เอกสารนี้คือ template `PROMPT_System_Skeleton_TEMPLATE.md` ที่เติมค่าครบแล้วสำหรับโปรเจกต์นี้
> ใช้เป็น context ตั้งต้นให้ AI Coding Agent ทำงานต่อบนโค้ดเดิม (ไม่ใช่สร้างใหม่จากศูนย์)

---

## 0. กฎเหล็ก

1. **โปรเจกต์นี้มีอยู่แล้ว** ที่ `d:\Portfolio\portfolio\` — แก้ไข/ต่อยอดจากไฟล์เดิม **ห้ามสร้างโฟลเดอร์ใหม่หรือ rewrite ทั้งไฟล์**
2. **ห้ามเปลี่ยน stack** เป็น React / Next.js / build tool ใด ๆ โดยไม่ถามก่อน — จุดขายของงานนี้คือเปิด `index.html` แล้วรันได้ทันที ไม่มี `npm install`
3. **ห้ามแก้เนื้อหาข้อเท็จจริง** (ชื่อ, เกรด, ปี, ชื่อโปรเจกต์, ลิงก์ social) ถ้าไม่ได้สั่ง — แก้ได้เฉพาะคำผิด/การสะกด
4. ภาษา UI: **ไทย** (เนื้อหา) + **อังกฤษ** (หัวข้อ section, nav) · ภาษาโค้ด/ตัวแปร/คอมเมนต์ใหม่: **อังกฤษ**
5. ทุก path ของรูปและ PDF ต้องเป็น **relative จาก root ของโปรเจกต์** (`images/...`, `pdf/...`) ห้ามใช้ `../`
6. ไม่มี backend → **ห้ามใส่ API key, token, หรือ secret ใด ๆ ลงในโค้ด** เพราะทุกอย่างเปิดอ่านได้จาก view-source

---

## 1. บริบทโปรเจกต์

**ระบบ:** เว็บ Portfolio ส่วนตัวแบบหน้าเดียว (single-page, scroll-based) แสดงประวัติ · ทักษะ · ผลงาน/กิจกรรม · เอกสาร workshop · การศึกษา · ใบรับรอง · ช่องทางติดต่อ

**เป้าหมาย:** ใช้ประกอบการสมัครฝึกงาน/สมัครงานสาย IT · Business Analyst · Developer — ให้ HR หรืออาจารย์เปิดครั้งเดียวแล้วเห็นภาพรวมครบภายใน 1 นาที

### ผู้ใช้ & บทบาท (Roles)

| Role | คำอธิบาย | การเข้าถึง |
|---|---|---|
| `VISITOR` | ผู้เข้าชมทั่วไป (HR, อาจารย์, เพื่อนร่วมงาน) | ไม่ต้องล็อกอิน · อ่านอย่างเดียว |
| `OWNER` | เจ้าของ portfolio (ณันธภัทร์ ลิ่มเจริญ) | แก้เนื้อหาโดยแก้ไฟล์ HTML ตรง ๆ แล้ว deploy ใหม่ |

> **ไม่มีระบบสมาชิก ไม่มีการล็อกอิน** — ผู้ใช้ทุกคนคือ anonymous visitor ที่สิทธิ์เท่ากัน

---

## 2. Technology Stack

| ชั้น | เทคโนโลยีที่ใช้จริง |
|---|---|
| Framework / Frontend | **ไม่มี framework** — HTML5 + CSS3 + Vanilla JavaScript (ES6) |
| ภาษา | HTML / CSS / JavaScript (`'use strict'`) |
| Backend / API | ✕ ไม่มี (static site ล้วน) |
| ORM | ✕ ไม่มี |
| Database | ✕ ไม่มี — เนื้อหาทั้งหมด hardcode อยู่ใน `index.html` |
| Cache / Queue | ✕ ไม่มี |
| Auth | ✕ ไม่มี |
| Password hashing | ✕ ไม่มี |
| Validation | ✕ ไม่มี (ไม่มีฟอร์มรับ input) |
| Realtime | ✕ ไม่มี |
| ไฟล์ / Export | PDF แบบ static ในโฟลเดอร์ `pdf/` (เปิดดู + ดาวน์โหลด) |
| การชำระเงิน | ✕ ไม่มี |
| งานตามเวลา (cron) | ✕ ไม่มี |
| Styling / UI | CSS ล้วน + CSS Custom Properties (design tokens) · ไม่มี Tailwind / ไม่มี CSS framework |
| ไอคอน | Font Awesome 6.5.0 (CDN: cdnjs) |
| ฟอนต์ | Google Fonts — `Inter` (เนื้อหา) + `Cormorant Garamond` (หัวข้อ) + `Outfit` |
| Animation | Intersection Observer API + CSS transition/keyframes |
| Test | ✕ ยังไม่มี (ดู §12) |
| Mobile | ✕ ไม่มีแอป — ใช้ responsive web แทน |
| Deploy | Static hosting (GitHub Pages / Netlify / Vercel) หรือเปิดไฟล์ `index.html` ตรง ๆ |

---

## 3. โครงสร้างโปรเจกต์ (ของจริง)

```
portfolio/
├── index.html                    # ทั้งเว็บอยู่ในไฟล์เดียว — 7 section
├── style.css                     # ~1,420 บรรทัด · design tokens + ทุก component (ล้าง CSS ที่ไม่ใช้แล้ว)
├── script.js                     # ~280 บรรทัด · interaction ทั้งหมด (ล้างโค้ดตายแล้ว P4)
├── README.md                     # วิธีเปิด/เพิ่มเนื้อหา/deploy (เขียนใน P6)
├── PROMPT_Portfolio.md           # เอกสารนี้
├── images/
│   ├── myku.png                  # รูปโปรไฟล์ (ใช้ใน About)
│   ├── myku1.png                 # (ยังไม่ถูกใช้)
│   ├── IoT.jpg                   # ผลงาน: เครื่องวัดแอลกอฮอล์
│   ├── INET.jpg                  # กิจกรรม: ดูงาน INET
│   ├── Zahira.jpg                # กิจกรรม: ดูงาน Zahira Farm
│   ├── skill_ai.jpg              # กิจกรรม: อบรม AI/Cyber
│   ├── *_certificate.jpg         # ใบรับรอง HackerRank 6 ใบ
│   └── favicon.svg               # ไอคอนแท็บเบราว์เซอร์ (สร้างใน P5)
└── pdf/
    ├── CV..pdf                   # CV ที่ลิงก์จาก navbar
    ├── CV.0.pdf                  # (ยังไม่ถูกใช้)
    ├── ระบบจองโต๊ะร้านอาหาร.pdf
    ├── ระบบจัดตารางเรียน.pdf      # เปลี่ยนชื่อแล้ว P5
    ├── ระบบช่วยบริหารเงิน.pdf
    └── Cheese.pdf                # การเทรน AI แยกประเภทชีส
```

---

## 4. Content Model (แทน Database Schema)

> ไม่มีฐานข้อมูล — "ตาราง" ของโปรเจกต์นี้คือ **บล็อก HTML ที่มีรูปแบบตายตัว**
> เวลาเพิ่มเนื้อหาใหม่ ให้ copy บล็อกเดิมแล้วเปลี่ยนค่า **ห้ามคิดโครงสร้างใหม่**

| "ตาราง" | คลาสหลัก | ฟิลด์ที่ต้องมีครบ |
|---|---|---|
| `Project` | `.project-card` | `<h3>` ชื่อ · `.project-img > img` (มี `alt`) · `<p>` คำอธิบาย · `.proj-stack > span` เครื่องมือที่ใช้ |
| `Document` | `.doc-card` | `.doc-card-head` (`.doc-icon` SVG + `.doc-kind` ชนิดเอกสาร) · `.doc-title` · `.doc-meta` (`จำนวนหน้า · PDF · ปี พ.ศ.`) · `.doc-actions` 2 ปุ่ม (ดูเอกสาร = `target="_blank"`, ดาวน์โหลด = `download`) |
| `Education` | `.tl-item` | `.tl-dot` · `.tl-year` (ช่วงปี ค.ศ.) · `.tl-place` · `<p>` เกรดเฉลี่ย ใน `<strong>` |
| `Certificate` | `.cert-card` | `.brings-icon` · `.cert-img` (**ต้องเพิ่ม `alt`**) · `<h4>` ชื่อ · `.cert-issuer` · `.cert-date` |
| `TechTile` | `.tech-tile` | `<svg data-logo="..." viewBox="0 0 w h"><use href="#logo-..."></svg>` + `<span>` ชื่อ · ของที่ไม่มีโลโก้แบรนด์ใช้ `<svg class="generic-icon">` ชี้ไป `#icon-*` ที่วาดเอง |
| `LogoItem` | `.logo-loop-item` | `<svg data-logo="..." viewBox="0 0 w h"><use href="#logo-..."></svg>` — วิ่งเป็น marquee · โลโก้นิยามเป็น `<symbol>` ใน sprite ที่หัวไฟล์ |
| `Contact` | `.contact-item` | `.brings-icon > i` · `.ci-label` · `.ci-val` · `href` (ถ้าเป็นลิงก์) |

### ข้อบังคับของ Content Model

- **ทุก `<img>` ต้องมี `alt` ที่สื่อความหมาย** (ครบทุกรูปแล้ว ✅ P2 — รูปใหม่ต้องใส่ด้วยเสมอ)
- ทุก `<a target="_blank">` ต้องมี `rel="noopener noreferrer"`
- ปีในส่วน Education ใช้ **ค.ศ.** · ปีใน `.doc-meta` ใช้ **พ.ศ.** — คงความต่างนี้ไว้ตามเดิม ห้ามแปลงข้ามกัน
- ทุก section ต้องมี `id` ที่ตรงกับ `href` ใน navbar และ mobile menu
- element ที่อยากให้ค่อย ๆ ปรากฏ ต้องใส่คลาส `reveal` (+ `reveal-delay` / `reveal-delay-2` เพื่อหน่วงเป็นลำดับ)

---

## 5. Business Rules / Core Logic

> ตรรกะทั้งหมดอยู่ใน `script.js` และเป็น **DOM interaction** ไม่ใช่ business logic เชิงคำนวณ

### ค่าตั้งต้น (กระจายอยู่ใน script.js)

```
NAVBAR_SCROLL_THRESHOLD  = 30    px   → navbar เปลี่ยนเป็นสถานะ .scrolled
BACKTOTOP_THRESHOLD      = 300   px   → ปุ่ม ↑ ปรากฏ
SCROLL_OFFSET            = 70    px   → ชดเชยความสูง navbar ตอน smooth scroll
REVEAL_THRESHOLD         = 0.1        → สัดส่วนที่ต้องเห็นก่อน element จะ reveal
TYPING_SPEED             = 70 ms/ตัว (พิมพ์) · 40 ms/ตัว (ลบ) · หยุด 1500 ms เมื่อพิมพ์จบ
RIPPLE_DURATION          = 600   ms
```

### กติกา

| # | กติกา |
|---|---|
| **BR-01** | Navbar ติดบนตลอดเวลา · เมื่อ `scrollY > 30` ให้เพิ่มคลาส `.scrolled` |
| **BR-02** | คลิกลิงก์ `#anchor` ใด ๆ ต้อง smooth scroll พร้อมหักลบ 70px ไม่ให้หัวข้อโดน navbar บัง |
| **BR-03** | ลิงก์ใน navbar ที่ตรงกับ section ปัจจุบัน ต้องได้คลาส `.active-link` และมีลิงก์ active ได้ทีละ 1 อันเท่านั้น |
| **BR-04** | เปิดเมนูมือถือ → hamburger ต้องกลายเป็นรูป X · ปิดเมนูได้ 3 ทาง: กดปุ่มซ้ำ, คลิกลิงก์ในเมนู, กด `Esc` |
| **BR-05** | element `.reveal` จะแสดงเมื่อเลื่อนมาถึงเท่านั้น แล้ว **unobserve ทันที** — animate ครั้งเดียวไม่ซ้ำ |
| **BR-06** | ข้อความ typing ใน About วนไม่รู้จบระหว่าง 2 ประโยค: พิมพ์ → หยุด 1.5 วิ → ลบ → ประโยคถัดไป |
| **BR-07** | แถบโลโก้ (`#logoTrack`) เลื่อนต่อเนื่องไม่มีรอยต่อ — JS โคลนชุดโลโก้ให้ได้**จำนวนคู่** และครึ่งแถบต้องกว้าง ≥ 1200px (animation เลื่อน -50% ของแถบ · ถ้าจำนวนชุดเป็นคี่จะกระตุก · ถ้าครึ่งแถบแคบกว่าช่องที่มองเห็นจะโผล่ช่องว่างท้ายแถบ) **ห้ามรันโค้ดโคลนนี้ซ้ำสองครั้ง** |
| **BR-08** | คลิกที่ `.clickable-image` เกิด ripple ที่ตำแหน่งเมาส์ แล้วลบ element ทิ้งใน 600ms (เดิมมีที่ `.brings-item` ด้วย แต่การ์ดนั้นถูกแทนด้วยไทล์โลโก้ไปแล้วตอน ISSUE-35) |
| **BR-09** | ปุ่ม Back-to-top แสดงเมื่อ `scrollY > 300` และเลื่อนกลับบนสุดแบบ smooth |
| **BR-10** | PDF ทุกไฟล์ต้องมีทั้งปุ่ม "ดู" (เปิดแท็บใหม่) และ "ดาวน์โหลด" เสมอ |
| **BR-11** | ทุก listener ที่ผูกกับ `scroll` ต้องใส่ `{ passive: true }` เพื่อไม่บล็อกการเลื่อน |
| **BR-12** | โค้ดที่อ้าง element ต้องเช็ก null ก่อนใช้เสมอ — เพราะ `script.js` โหลดโดยไม่มี `defer` และมี element ที่อาจถูกคอมเมนต์ทิ้ง |

### Test cases สำคัญ (manual QA — ยังไม่มี automated test)

| อินพุต | ผลลัพธ์ที่ถูกต้อง |
|---|---|
| เปิดหน้าเว็บครั้งแรก | รูปทุกรูปขึ้นครบ **ไม่มีไอคอนรูปแตก** · console ไม่มี error |
| เลื่อนลงจนสุดหน้า แล้วเลื่อนกลับขึ้น | ทุก section reveal ครบ · ไม่มี section ไหนค้างจาง |
| กด `Ctrl + D` | ไม่เกิด JavaScript error และเบราว์เซอร์เปิด bookmark ได้ตามปกติ (✅ แก้ไขแล้ว P1) |
| ย่อจอเหลือ 375px | ไม่มี horizontal scroll · hamburger ทำงาน · ตัวหนังสือไม่ล้นกรอบ |
| กด Tab ไล่ทั้งหน้า | โฟกัสไปตามลำดับที่อ่านได้ · มองเห็น focus ring ทุกจุด |
| คลิก "ดู" ทุกไฟล์ PDF | เปิดแท็บใหม่ได้ครบ 4 ไฟล์ + CV |
| กด Esc ตอนเมนูมือถือเปิด | เมนูปิด · hamburger กลับเป็น 3 ขีด |

---

## 6. State Machine

> **ไม่มี entity ที่มีสถานะทางธุรกิจ** (ไม่มี order / ticket / approval) — ตัดส่วน state machine ตามระบบทิ้ง
> เหลือเพียง UI state ง่าย ๆ ที่ต้องคุมให้สอดคล้องกัน:

```
navbar:      [top] ──(scrollY > 30)──► [scrolled] ──(scrollY ≤ 30)──► [top]
mobileMenu:  [closed] ──(hamburger | Esc | คลิกลิงก์)──► [open] ──►◄── [closed]
reveal:      [hidden] ──(เข้า viewport 10%)──► [visible]   ← ทางเดียว ไม่ย้อนกลับ
typing:      [typing] ──(จบประโยค)──► [pause 1.5s] ──► [deleting] ──► [typing ประโยคถัดไป]
```

**Guard:**

- `reveal` เป็น terminal state — เมื่อ `visible` แล้วต้อง `unobserve` ห้ามกลับไป hidden
- สถานะเมนูมือถือต้องซิงก์ระหว่างตัวแปร `menuOpen`, คลาส `.open`, และรูปร่าง hamburger เสมอ ห้ามหลุดจากกันแม้ทางใดทางหนึ่ง

---

## 7. External Integrations

**ไม่มี API ภายนอกและไม่มี payment provider** — พึ่งพาแค่ CDN 3 แห่ง:

| ทรัพยากร | แหล่ง | ใช้ทำอะไร | ถ้าโหลดไม่ได้ |
|---|---|---|---|
| Font Awesome 6.5.0 | `cdnjs.cloudflare.com` | ไอคอนทุกจุดในหน้า | ไอคอนหายทั้งหมด → ควรมี fallback |
| Google Fonts (Inter) | `fonts.googleapis.com` | ฟอนต์เนื้อหา | ตกไปใช้ `sans-serif` |
| Cormorant Garamond / Outfit | `fonts.googleapis.com` | หัวข้อ (`--font-display`) / ตัวเน้น (`--font-body`) | ตกไปใช้ Georgia / sans-serif |

> ลิงก์ภายนอก (Instagram / Facebook / GitHub) เป็น `<a>` ธรรมดา ไม่มีการเรียก API

---

## 8. Scheduled Jobs

**✕ ไม่มี** — ไม่มี worker, ไม่มี cron, ไม่มี background job

ข้อมูลเดียวที่อัปเดตอัตโนมัติคือปีใน footer (`.footer-copy` ใน `script.js`) ซึ่งคำนวณตอนโหลดหน้า แต่ **element `.footer-copy` ยังไม่มีจริงใน HTML** จึงไม่มีผลใด ๆ

---

## 9. รายละเอียดหน้าจอ (ทีละ section)

### 🔹 Navbar (คงที่ทุกหน้าจอ)

- **หน้าที่:** นำทางไป 6 section + ปุ่ม View CV
- **องค์ประกอบ:** โลโก้ `Portfolio.` · ลิงก์ 6 อัน · ปุ่ม View CV · hamburger (จอเล็ก)
- **การทำงาน:** ติดบน · `.scrolled` เมื่อเลื่อน · `.active-link` ตาม section
- **✅ แก้แล้ว (P3):** เพิ่ม `#workshop` เข้าทั้ง navbar และ mobile menu (วางหลัง Projects ตามลำดับหน้าจริง) · ทุก section มีลิงก์ครบ 7/7

### 🔹 About (`#about`)

- **หน้าที่:** แนะนำตัว — นิสิต ม.เกษตรศาสตร์ สาขาเทคโนโลยีสารสนเทศ สนใจ Process / Requirement
- **องค์ประกอบ:** ข้อความ typing สลับ 2 ประโยค · ย่อหน้าแนะนำตัว 2 ย่อหน้า · pill 3 อัน (Problem Solver / Detail-Oriented / Quick Learner) · รูปโปรไฟล์คลิกได้ (ripple)
- **Empty/Error state:** ถ้ารูปโหลดไม่ได้ ต้องยังเห็นกรอบ avatar ไม่ใช่ไอคอนรูปแตก

### 🔹 Skills & Technology (`#brings`)

- **หน้าที่:** แสดงทักษะ 4 หมวด + แถบเทคโนโลยีวิ่ง
- **องค์ประกอบ:** Logo Loop 6 โลโก้วิ่งวน (JavaScript, Node.js, Python, Docker, C, Java) เป็นแบนเนอร์นำ · **กริดไทล์โลโก้ 32 ใบ แบ่ง 4 หมวด** — Languages & Frameworks 10 · Database & Backend 5 · Design & UX 5 · Tools 12 · ใน 32 ใบนี้เป็นโลโก้แบรนด์จริง 27 และไอคอนเส้นที่วาดเอง 5 (Wireframing, Prototype, Visio, Netpie, Wokwi)
- **✅ แก้แล้ว (P1):** `</h4>` ที่เกินมา และ `<div class="container">` ที่ไม่ถูกปิด

### 🔹 Projects & Activities (`#projects`)

- **หน้าที่:** ผลงานและกิจกรรม 4 รายการ
- **องค์ประกอบ:** IoT เครื่องวัดแอลกอฮอล์ · ดูงาน INET · ดูงาน Zahira Farm · อบรม AI & Cyber
- **การทำงาน:** hover แล้วการ์ดเอียงตามเมาส์เล็กน้อย (tilt)
- **✅ แก้แล้ว (P3):** การ์ด "เปิดโลกทักษะ AI" มี `.proj-stack` แล้ว (`AI` · `Cybersecurity`) · ครบ 4/4 ใบ · แก้ `network` → `Network` ให้ตัวพิมพ์ตรงกับใบอื่น

### 🔹 Workshop (`#workshop`)

- **หน้าที่:** เอกสารงานวิเคราะห์ระบบ 4 ฉบับให้ดู/ดาวน์โหลด
- **องค์ประกอบ:** ระบบจองโต๊ะร้านอาหาร · ระบบจัดตารางเรียน · ระบบช่วยบริหารเงิน · การเทรน AI แยกประเภทชีส
- **✅ แก้แล้ว (P3):** มีลิงก์ใน navbar แล้ว
- **✅ แก้แล้ว (P4):** รวม SVG ไอคอนเอกสารเป็น `<symbol id="icon-doc">` ครั้งเดียวต้น `<body>` แล้วเรียกด้วย `<use href="#icon-doc">` ทั้ง 4 จุด

### 🔹 Education (`#education`)

- **หน้าที่:** timeline การศึกษา 2 ระดับ
- **องค์ประกอบ:** ม.เกษตรศาสตร์ (2023–ปัจจุบัน · GPA 2.82) · ร.ร.พระปฐมวิทยาลัย (2020–2023 · ศิลป์-คำนวณ · GPA 3.60)
- **✅ แก้แล้ว (P3):** คืน `<h3>ปริญญาตรี</h3>` (เดิมถูกคอมเมนต์ไว้) ให้รูปแบบตรงกับรายการมัธยม — `<h3>` = ระดับการศึกษา, `.tl-place` = สถาบัน · คณะ · สาขา
- **✅ แก้แล้ว (P2):** สะกด "เกษตรศาสตร์"

### 🔹 Certificates (`#certificates`)

- **หน้าที่:** ใบรับรอง HackerRank 6 ใบ (CSS, Java, JavaScript, Node.js, SQL Intermediate, Python — ปี 2024)
- **✅ แก้แล้ว (P2):** ใส่ `alt` ครบทั้ง 6 ใบ (รูปแบบ "ใบรับรอง <ชื่อ> จาก HackerRank")

### 🔹 Contact (`#contact`)

- **หน้าที่:** ช่องทางติดต่อ 4 ช่อง — Instagram, Facebook, GitHub, Gmail
- **✅ แก้แล้ว (P2):** "Instagram" · ข้อความชวนติดต่อ · แท็ก `</i>` ที่เกิน
- **✅ แก้แล้ว (P3):** Instagram/Facebook เปิดแท็บใหม่พร้อม `rel="noopener noreferrer"` เหมือน GitHub แล้ว (3/3)
- **✅ แก้แล้ว (P4):** Gmail เป็น `<a href="mailto:...">` แล้ว — ช่องทางติดต่อเป็นลิงก์ครบ 4/4 · `copyToClipboard()`/`showToast()` ที่ไม่มีใครเรียกถูกลบทิ้ง (ถ้าอยากได้ปุ่มคัดลอกอีเมลจริง ๆ ค่อยเขียนใหม่พร้อม UI)

### 🔹 Footer

- **✅ แก้แล้ว (P2):** `.Profolio` → `Portfolio<span class="dot">.</span>` ให้ตรงกับโลโก้ใน navbar

---

## 10. Security & Quality Requirements (ปรับให้ตรงกับ static site)

รายการที่ **ไม่เกี่ยวกับโปรเจกต์นี้** เพราะไม่มี server/DB/auth: session cookie, password hashing, `sessionVersion`, RBAC, server-side validation, parameterized query, rate limiting, CSRF, audit log, DB user สิทธิ์จำกัด

รายการที่ **ต้องทำจริง**:

- [x] ทุก `target="_blank"` ต้องมี `rel="noopener noreferrer"` (ครบ 6/6 แล้ว ✅ P2)
- [ ] ไม่มี secret / API key / token ในโค้ด — ทุกอย่างเปิดอ่านได้จาก view-source
- [ ] ไม่ใช้ `innerHTML` กับข้อมูลที่มาจากภายนอก (ปัจจุบันใช้เฉพาะกับ static markup ของตัวเอง = ปลอดภัย)
- [ ] ทบทวนข้อมูลส่วนตัวบนหน้า public ก่อน deploy — ตอนนี้เปิดเผยอีเมล, Instagram, Facebook, GitHub และ CV ซึ่งเป็นความตั้งใจ **แต่ CV มักมีเบอร์โทร/ที่อยู่ ควรเช็กว่าเปิดเผยเท่าที่ยอมรับได้**
- [ ] ตรึงเวอร์ชัน CDN แบบเจาะจง (ทำแล้วสำหรับ Font Awesome `6.5.0`) และควรเพิ่ม SRI hash
- [ ] Console ต้องสะอาด ไม่มี error ตอนโหลดหรือใช้งานปกติ
- [ ] `.gitignore` — อย่า commit ไฟล์ส่วนตัวที่ไม่ได้ตั้งใจเผยแพร่

---

## 11. UI/UX

### Design Tokens (จาก `style.css` — ห้ามใส่สี hardcode ใหม่ ให้ใช้ตัวแปรเหล่านี้)

```
--cream   #F0F4FA   พื้นหลังหลัก (off-white อมฟ้า)
--white   #FFFFFF   พื้นการ์ด
--sand    #E4EBF5   พื้นรอง (.section-alt)
--accent  #C9960C   Gold — สีเน้น
--accent2 #1a2a5e   Deep Indigo
--text    #0F1B42   Navy เข้ม
--muted   #5C6E9A   ข้อความรอง
--border  #D8E2F0   เส้นขอบ
--radius  14px  ·  --radius-lg 22px
--trans   280ms cubic-bezier(0.4, 0, 0.2, 1)
```

- **Typography:** body = Inter 17px / line-height 1.8 / letter-spacing 0.2px · หัวข้อใช้ `--font-display`
- **Dark mode: ✕ ไม่มี** — เจ้าของตัดสินใจถอดออกทั้งหมดใน P6 (ลบ `body.dark-mode`, `.theme-toggle`, ปุ่ม toggle และโค้ด JS ที่คอมเมนต์ค้างไว้) เว็บเป็นธีมสว่างอย่างเดียว **ถ้าจะทำใหม่ต้องเขียนใหม่ทั้งชุด**
- **Responsive breakpoints ที่ใช้อยู่:** 1040px ×1 (บีบ nav 7 ลิงก์ · เดิม 1150 ย้ายลงตอน ISSUE-33) · 960px ×2 (สลับไป hamburger) · 768px ×2 · 600px ×3 · 560px ×1 → **ยังไม่รวบ** เพราะการย้าย rule ข้าม breakpoint เปลี่ยนหน้าตาจริงและตรวจไม่ได้ถ้าไม่เปิดเบราว์เซอร์ — ควรทำตอนเปิดหน้าดูอยู่
- **Motion:** reveal on scroll · marquee · typing · ripple · card tilt — **✅ เคารพ `prefers-reduced-motion` แล้ว (P5)** ทั้งฝั่ง CSS (ปิด animation/transition, `.reveal` เห็นทันที, marquee หยุด) และฝั่ง JS (smooth scroll → `auto`, typing แสดงประโยคแรกนิ่ง ๆ)
- **Accessibility เป้าหมาย WCAG AA:** ✅ ทุกรูปมี `alt` (P2) · ✅ focus ring ผ่าน `:focus-visible` สีทอง outline 2px (P5) · ✅ ปุ่มมี `aria-label` · ⚠ ยังไม่ได้ทดสอบว่าเนื้อหาไทยอ่านออกที่ 375px จริงไหม (ต้องเปิดเบราว์เซอร์)

---

## 12. Deliverables (checklist)

- [ ] เปิด `index.html` แล้วทุกอย่างทำงานครบ — **ไม่มีรูปแตก ไม่มี console error**
- [ ] แก้ปัญหาทั้งหมดใน §14
- [x] เพิ่ม `#workshop` เข้า navbar และ mobile menu (✅ P3)
- [x] SEO/แชร์ลิงก์: description · Open Graph + Twitter card · favicon SVG · `<title>` มีชื่อแล้ว (✅ P5 — **ยังต้องใส่ `og:url` เต็มหลัง deploy**)
- [x] `prefers-reduced-motion` สำหรับทุก animation (✅ P5 · CSS + JS)
- [x] ทำความสะอาด `script.js` — ลบโค้ดที่อ้าง element ที่ไม่มีจริงครบทุกตัว (✅ P4 · 391 → 274 บรรทัด)
- [x] รวม IntersectionObserver 2 ตัวที่ทำงานซ้ำกันบน `.reveal` ให้เหลือตัวเดียว (✅ P4)
- [x] `README.md` — วิธีเปิด, วิธีเพิ่มผลงาน/ใบรับรอง/PDF ใหม่, กฎที่ต้องรักษา, วิธี deploy, เช็กลิสต์ก่อนเผยแพร่ (✅ P6)
- [ ] ทดสอบด้วยมือครบทุกเคสใน §5 บน Chrome + มือถือ

---

## 13. ลำดับการทำงาน (ทำทีละเฟส — รายงานจบเฟส แล้วรอคำสั่งก่อนไปต่อ)

1. ~~**P1 — ทำให้หน้าเว็บไม่พัง:**~~ ✅ **เสร็จแล้ว 4 ก.ย. 2026** — แก้ path รูป, ปิดแท็กที่ขาด, แก้ `themeToggle` error *(ISSUE-01 ถึง 04)*
2. ~~**P2 — ความถูกต้องของเนื้อหา:**~~ ✅ **เสร็จแล้ว 4 ก.ย. 2026** — แก้คำสะกดทั้งหมด, เพิ่ม `alt`, เพิ่ม `rel="noopener"` *(ISSUE-05 ถึง 11)*
3. ~~**P3 — โครงสร้างครบถ้วน:**~~ ✅ **เสร็จแล้ว 4 ก.ย. 2026** — เพิ่ม Workshop เข้าเมนู, ทำ Education/Projects ให้สมมาตร, โหลดฟอนต์ที่ CSS เรียกใช้
4. ~~**P4 — ทำความสะอาดโค้ด:**~~ ✅ **เสร็จแล้ว 4 ก.ย. 2026** — ลบโค้ดตายใน `script.js`, รวม observer ที่ซ้ำ, รวม SVG ซ้ำเป็น `<use>`
5. **P5 — ขัดเงา:** ✅ SEO meta + favicon · ✅ `prefers-reduced-motion` · ✅ focus ring · ⬜ **รวบ breakpoint (ยังไม่ทำ — ต้องเปิดเบราว์เซอร์ดู)**
6. ~~**P6 — ตัดสินใจเรื่อง Dark mode:**~~ ✅ **เสร็จแล้ว 4 ก.ย. 2026** — เจ้าของเลือก **ถอดออกทั้งหมด** · เขียน `README.md` แล้ว

---

## 14. ปัญหาที่พบในโค้ดปัจจุบัน (ยืนยันจากการอ่านโค้ดจริง)

| # | ระดับ | ไฟล์ | ปัญหา |
|---|---|---|---|
| **ISSUE-01** | ✅ **แก้แล้ว** (P1) | `index.html` 11 จุด | `src="../images/..."` ชี้ออกนอกโปรเจกต์ → รูปแตกทั้งเว็บ · **แก้เป็น `images/...` แล้ว ยืนยันไฟล์ปลายทางมีครบ 11 รูป** |
| **ISSUE-02** | ✅ **แก้แล้ว** (P1) | `index.html:10-13` | CSS เรียก `Cormorant Garamond` / `Outfit` แต่ HTML ไม่ได้โหลด · **เพิ่มลงคำขอ Google Fonts เดียวกับ Inter แล้ว + preconnect `fonts.gstatic.com` · ยืนยัน HTTP 200 ส่งกลับครบ 3 family** |
| **ISSUE-03** | ✅ **แก้แล้ว** (P1) | `script.js:289` | `themeToggle?.click()` อ้างตัวแปรในบล็อกคอมเมนต์ → `Ctrl+D` เกิด ReferenceError · **ลบ shortcut ทิ้งพร้อมคอมเมนต์ชี้ไป P6 · `Ctrl+D` กลับไปเป็น bookmark ของเบราว์เซอร์ตามปกติ** |
| **ISSUE-04** | ✅ **แก้แล้ว** (P1) | `index.html:101, 118` | `<div class="container">` ของ `#brings` ไม่ถูกปิด + `</h4>` เกิน 1 ตัว · **แก้แล้ว ยืนยันแท็กสมดุลทั้งไฟล์ (div 125/125, section 7/7, h4 10/10)** |
| **ISSUE-05** | ✅ **แก้แล้ว** (P2) | `index.html:341` | "มหาวิทยาลัยเกษตรศาตร์" → **เกษตรศาสตร์** |
| **ISSUE-06** | ✅ **แก้แล้ว** (P2) | `index.html:458` | "มีโปรเจกต์นพูดคุยเพิ่มเติม?" → **"สนใจร่วมงานหรืออยากพูดคุยเพิ่มเติม?"** |
| **ISSUE-07** | ✅ **แก้แล้ว** (P2) | `index.html:463` | "Intragram" → **Instagram** |
| **ISSUE-08** | ✅ **แก้แล้ว** (P2) | `index.html:496` | footer `.Profolio` → **`Portfolio<span class="dot">.</span>`** ให้ตรงกับ navbar |
| **ISSUE-09** | ✅ **แก้แล้ว** (P2) | `index.html` cert ทั้ง 6 ใบ | ใส่ `alt` ครบแล้ว · ยืนยัน `<img>` ทั้งไฟล์ไม่มีที่ขาด `alt` เลย |
| **ISSUE-10** | ✅ **แก้แล้ว** (P2) | `index.html` 6 จุด | เพิ่ม `rel="noopener noreferrer"` ครบ 6/6 |
| **ISSUE-11** | ✅ **แก้แล้ว** (P2) | `index.html:461, 468` | ลบ `</i>` ที่เกิน · ยืนยันแท็ก `<i>` สมดุล 14/14 |
| **ISSUE-12** | ✅ **แก้แล้ว** (P4) | `script.js:78-90` | ย้าย scroll listener เข้าไปอยู่ใน `if (backToTopBtn)` · เปลี่ยน if/else เป็น `classList.toggle()` |
| **ISSUE-13** | ✅ **แก้แล้ว** (P4) | `script.js` | ลบโค้ดตายครบทุกบล็อก: `.hero-tagline` · `.hero-bg-shape` · `.hc-num` + `animateCounter()` · `.skill-bar` · `.footer-copy` — ยืนยันไม่เหลืออ้างอิงสักตัว |
| **ISSUE-14** | ✅ **แก้แล้ว** (P4) | `script.js:147` | เหลือ IntersectionObserver ตัวเดียว (เก็บตัวที่ `unobserve` ตาม BR-05) เติมคลาส `visible` ซึ่งตรงกับ `.reveal.visible` ใน CSS — การแสดงผลเหมือนเดิม |
| **ISSUE-15** | ✅ **แก้แล้ว** (P4) | `script.js` | ลบ `showToast()` / `copyToClipboard()` ที่ไม่มีใครเรียก · Gmail เปลี่ยนเป็นลิงก์ `mailto:` แทน |
| **ISSUE-16** | ✅ **แก้แล้ว** (P3) | `index.html:26, 61` | เพิ่ม `#workshop` เข้า navbar + mobile menu · ยืนยันทุก section มีลิงก์ครบ 7/7 |
| **ISSUE-17** | ✅ **แก้แล้ว** (P5) | `pdf/` | เปลี่ยนชื่อไฟล์เป็น `ระบบจัดตารางเรียน.pdf` + แก้ลิงก์ทั้ง 2 จุด · ยืนยันลิงก์ทุกอันในหน้าชี้ไฟล์ที่มีอยู่จริง |
| **ISSUE-18** | ✅ **แก้แล้ว** (P5) | `index.html:7-25` | `<title>` มีชื่อ · `meta description` · Open Graph + Twitter card · `favicon.svg` (โลโก้ P navy + จุดทอง ตาม design token) · **เหลือใส่ `og:url` เต็มหลัง deploy** |

| **ISSUE-19** | ✅ **แก้แล้ว** (P2) | `index.html:164` | หัวข้อผลงานสะกด "เครื่องวัดแฮลกอฮอล์" ทั้งที่เนื้อหาบรรทัดถัดมาสะกด "แอลกอฮอล์" ถูก — **พบใหม่ระหว่างทำ P2** |
| **ISSUE-20** | ✅ **แก้แล้ว** (P3) | `index.html:465, 472` | Instagram/Facebook เปิดแท็บใหม่พร้อม `rel` เหมือน GitHub แล้ว (3/3) |

| **ISSUE-21** | ✅ **แก้แล้ว** (P3) | `index.html:218` | หัวข้อการ์ดสะกด "เปิดโลกทักษะ Aiและทักษะทางไซเบอร์" (ตกเว้นวรรค + `Ai`) → **"AI และ"** — **พบใหม่ระหว่างทำ P3** |
| **ISSUE-22** | ✅ **แก้แล้ว** (P3) | `style.css:253` | `.mobile-menu.open` ตั้ง `max-height: 400px` คู่กับ `overflow: hidden` แต่ลิงก์ที่ 7 ทำให้เนื้อหาสูง ~429px → ลิงก์ Contact โดนตัดและกดไม่ได้ · **ขยายเป็น 480px** — **ผลข้างเคียงจากการเพิ่มลิงก์ใน P3** |

| **ISSUE-23** | ✅ **แก้แล้ว** | `style.css` | ลบ CSS ที่ไม่มีทางแมตช์ออกหมด: **110 rule · 4 `@keyframes` (floatSlow, bounce, slideIn, slideOut) · 3 ตัวแปร (`--shadow-md/lg/xl`) · คอมเมนต์หัวข้อกำพร้า 15 บรรทัด** → ไฟล์ **2,342 → 1,421 บรรทัด (-39%)** · ยืนยัน rule ที่ใช้อยู่ 180 อันครบเท่าเดิม ไม่มีคลาสไหนในหน้าเสียสไตล์ · **เก็บ 15 rule ที่ผูกกับ markup ที่คอมเมนต์ไว้** (`.section-title`, `.proj-tag`, `.tag-*`, `.pc-header`, `.nav-cv-btn`) เพราะถ้าเปิดกลับมาใช้จะกระทบ |

| **ISSUE-24** | ✅ **แก้แล้ว** | `index.html:8, 10, 11, 17` | ชื่อที่ผมเดาไว้ **"Nanthapat Tangpun" นามสกุลผิด** · เจ้าของยืนยันชื่อจริงคือ **ณันธภัทร์ ลิ่มเจริญ** — แก้ครบทั้ง `<title>`, `description`, `author`, `og:title` และหัวเอกสาร/README |
| **ISSUE-25** | ✅ **แก้แล้ว** | `pdf/` · `images/` | เปลี่ยน `CV..pdf` → **`CV.pdf`** (แก้ลิงก์ 2 จุด รวมอันที่อยู่ในคอมเมนต์) · ลบไฟล์ที่ไม่ถูกลิงก์: `pdf/CV.0.pdf` (CV เก่า 192KB) และ `images/myku1.png` (รูปเดิมขนาดเล็ก 212KB) |

| **ISSUE-26** | ✅ **แก้แล้ว** (P6) | `index.html` · `script.js` · `style.css` | Dark mode ค้างอยู่ครึ่ง ๆ กลาง ๆ (CSS ครบแต่ปุ่มถูกคอมเมนต์) · **ถอดออกหมดตามที่เจ้าของตัดสินใจ**: ปุ่ม `#themeToggle` · บล็อก JS ที่คอมเมนต์ไว้ 28 บรรทัด · `body.dark-mode` · `.theme-toggle` 3 rule — ยืนยันไม่เหลือคำว่า theme-toggle/dark-mode/themeToggle ในไฟล์ไหนเลย |

| **ISSUE-27** | ✅ **แก้แล้ว** | `style.css:1323-1372` | เพิ่มสไตล์ `.btn` (พื้นทึบ `--accent2` · hover เป็นทอง `--accent` · ยกขึ้น 2px) และ `.btn-outline` (ขอบบาง พื้นโปร่ง · hover ขอบทอง) · วาง `.btn-outline` หลัง `.btn` เพื่อให้ทับค่าได้ · `.btn-sm` ที่มี `!important` ยังคุมขนาดเหมือนเดิม |

| **ISSUE-28** | ✅ **แก้แล้ว** | `style.css:1309` | `.doc-name` ใช้ `color: var(--text-dark)` แต่ **ไม่เคยมีการประกาศตัวแปรนี้** → property ใช้ไม่ได้ สีตกไป inherit · แก้เป็น `var(--text)` · **พบระหว่างแก้ ISSUE-27** · ไล่ทั้งไฟล์แล้วไม่เหลือ `var()` ที่ชี้ตัวแปรไม่มีอยู่จริงอีก |

| **ISSUE-29** | ✅ **แก้แล้ว** | `images/myku.jpg` | รูปโปรไฟล์เดิม **1,251 KB** (PNG 838×1140 · ช่อง alpha ทึบทั้งภาพ = เปลืองฟรี) · ย่อเป็น **760×1034 JPEG q85 = 71.6 KB (-94%)** ขนาด 2× ของที่แสดงจริง (คอลัมน์ `.about-grid` กว้าง 380px) · เปลี่ยนนามสกุลเป็น `.jpg` และแก้ลิงก์ทั้ง `<img>` และ `og:image` |

| **ISSUE-30** | ⏸️ **พักไว้** (เจ้าของสั่งพัก 4 ก.ย. 2026 · จะกลับมาทำ) | `images/` 10 ไฟล์ | รูปที่เหลือรวม **~6.5 MB** ใหญ่เกินที่แสดงจริงหลายเท่า: ใบรับรอง 6 ใบ 2481×1890 (`css_certificate.jpg` ถึง 4961×3780) แต่แสดงแค่ ~340px ในกริด 3 คอลัมน์ · `INET.jpg`/`Zahira.jpg`/`skill_ai.jpg` 2048px แต่แสดง ~500px · **แผน:** ใบรับรอง→900px, รูปกิจกรรม→1200px, JPEG q85 คาดเหลือ ~1.3 MB (-80%) · **ต้องถามก่อนลงมือ:** อยากให้ใบรับรองคลิกซูมอ่านได้ไหม (ตอนนี้ไม่มี lightbox ความละเอียดสูงจึงไม่ถูกใช้) |

| **ISSUE-31** | ✅ **แก้แล้ว** | `index.html` sprite + Contact | เปลี่ยนไอคอนซองจดหมายเป็น **โลโก้ Gmail ตัวจริง** (SVG จาก thesvg.dev · CC0-1.0 ไม่ต้องใส่เครดิต) · เพิ่มเป็น `<symbol id="icon-gmail">` ใน sprite เดิม เรียกด้วย `<use>` ตามแพตเทิร์นเดียวกับ `icon-doc` · ยืนยัน path ครบ 5 เส้นตรงกับต้นฉบับและ sprite เป็น XML ที่ถูกต้อง |
| **ISSUE-32** | ✅ **แก้แล้ว** | `index.html` sprite · `style.css` | เปลี่ยนไอคอนติดต่อทั้ง 4 เป็น **โลโก้แบรนด์สีจริงบนชิปขาว 48×48** (thesvg.dev · CC0-1.0) · แก้ปัญหาที่รากคือ **สีแบรนด์ contrast ไม่ผ่านบนพื้น navy** (FB 2.55 · IG 2.49 · GitHub 1.66 : 1) พอวางบนชิปขาวได้ 4.82 / 4.34 / 17.89 : 1 · **ต้องเติม prefix ให้ id ภายใน SVG** เพราะ instagram กับ facebook ประกาศ `id="a"` ชนกัน (กลายเป็น `ig-*` / `fb-*`) และเพิ่ม `xmlns:xlink` ให้ sprite เพราะโลโก้ Instagram ใช้ `xlink:href` |
| **ISSUE-33** | ✅ **แก้แล้ว** | `style.css:111-160, 508-517` · `script.js:201` | **เมนูบนล้นกรอบ 83px** — พื้นที่จริงมี 1020px (`.nav-inner` 1100 − padding 80) แต่ โลโก้ 121.5 + เมนู 856.5 + ปุ่ม CV 125.5 = **1103.4px** `justify-content: space-between` จึงไม่เหลือที่แจก **ช่องว่างริมกลายเป็น 0 ทั้งสองข้าง** (โลโก้ชน About · Contact ชนปุ่ม View CV) · ต้นเหตุคือระยะซ้อนกัน 3 ชั้น `gap 32 + margin 4+4 + padding 12+12` = **64px ต่อคำ** กินไป 248px กับความว่างเปล่า (ตลกร้ายคือ media query เดิมที่ 1150px ทำให้จอ**แคบ**กว่ากลับพอดีกว่า — สัดส่วนกลับหัว) · **แก้:** เหลือระบบเดียว `gap: 6px` + `padding: 8px 14px` ตัด `margin` ทิ้ง → ระยะระหว่างคำ 34px · เหลือช่องว่างริมข้างละ **50.3px** · ย้ายจุดบีบ 1150 → **1040px** (จุดที่แถบเริ่มแคบกว่ากรอบ 1100 จริง ๆ) · วัดแล้วไม่ล้นทุกช่วงตั้งแต่ 961px ขึ้นไป จุดคับสุด (961 และ 1041) เหลือริมข้างละ 20.8px · **พร้อมกันนั้นเปลี่ยนหน้าตาเป็นแบบ editorial ตามที่เจ้าของเลือก:** ตัดกล่องพื้นหลัง/แสง hover/ระลอกคลื่นออก เหลือ **เส้นทอง `var(--accent)` 2px ใต้คำ** สีเดียวกับป้าย `.section-label` ที่อยู่ใต้แถบพอดี · ใช้ตัวแปร `--nav-pad` หักซ้าย-ขวาให้เส้นยาวเท่าคำเป๊ะ (วัดได้ 74.34px เท่าคำว่า Education ทั้งก่อนและหลังบีบระยะ) จอแคบจึงเปลี่ยนแค่ตัวแปรที่เดียว · สถานะยังบอกด้วยสีตัวอักษรด้วย (`--muted` → `--accent2`) ไม่ได้พึ่งเส้นอย่างเดียว · **ผลข้างเคียงที่ต้องตามเก็บ:** ระลอกคลื่นวงกลมใน `script.js` เคยอาศัย `overflow: hidden` ของกล่องกั้นไว้ พอเอากล่องออกมันจะล้นไปทับลิงก์ข้าง ๆ → จำกัดให้เหลือแค่ `.mobile-menu a` (แถวเต็มความกว้าง มีพื้นให้คลื่นวิ่ง) |
| **ISSUE-34** | ✅ **ทำแล้ว** (เจ้าของสั่ง) | `index.html` sprite + `#brings` · `style.css` · `script.js` | เปลี่ยนแถบ **pill ข้อความ → Logo Loop โลโก้จริง** 6 ตัว (thesvg.org · MIT · โหลดผ่าน jsDelivr `gh/glincker/thesvg@main/public/icons/<ชื่อ>/default.svg`) เพิ่มเป็น `<symbol id="logo-*">` ใน sprite เดิม · **เจอบั๊กที่ต้องแก้ระหว่างทาง 4 ข้อ:** **(1) โครงสร้าง** `.brings-visual-grid` (การ์ดทักษะ 4 ใบ) ซ้อนอยู่ใน `.tech-scroll` ที่มี `overflow: hidden` มาแต่เดิม — เงา/การยกตัวตอน hover ของการ์ดถูกตัด และถ้าใส่ mask ให้ขอบแถบจาง การ์ดทั้งกริดจะจางตามไปด้วย ย้ายออกมาเป็นพี่น้องกันใน `.container` แล้ว · **(2) marquee กระตุกทุกรอบ** ของเดิมใช้ `gap` ของ flex ซึ่งมีแค่ n−1 ช่อง แต่ animation เลื่อน -50% ของความกว้างทั้งแถบ ตัวเลขไม่ลงตัว กระตุกครึ่งช่อง (8px) ทุกรอบ — ย้ายไปใช้ `margin-right` ของแต่ละอันแทน ทุกอันมีช่องของตัวเอง ครึ่งแถบ = 1 ชุดพอดี **วัดได้ diff = 0.000** · **(3) โคลนไม่พอ** 1 ชุดกว้าง 631px แต่ช่องที่มองเห็นกว้างถึง 1020px พอ animation วนกลับจะเห็นช่องว่างท้ายแถบ — เปลี่ยนเป็นคำนวณจำนวนชุด (เลขคู่) จากเป้า 1200px ได้ 4 ชุดบนจอกว้าง 6 ชุดบนจอแคบ · **(4) โลโก้เพี้ยน 2 ตัว** Docker กลายเป็นสีดำ เพราะ `fill="#008fe2"` อยู่บนแท็ก `<svg>` ต้นฉบับ (path ข้างในไม่มี fill ของตัวเอง) และถูกทิ้งตอนตัดมาเป็น `<symbol>` · Python เหลือแค่เศษมุมล่างขวา เพราะ viewBox เป็น `16 16 32 32` ไม่ได้เริ่มที่ 0 0 พอใส่ viewBox เดียวกันทั้งบน `<svg>` ข้างนอกและ `<symbol>` `<use>` ถูกวางที่จุด (0,0) ซึ่งอยู่นอกกรอบที่มองเห็น — แก้โดยยก attribute ของ `<svg>` ต้นฉบับขึ้นไปไว้บน `<symbol>` และให้ `<svg>` ข้างนอกใช้ `0 0 กว้าง สูง` เสมอ · เพิ่ม mask ให้ขอบสองข้างจางแทนที่จะตัดกลางโลโก้ · หยุดวิ่งเมื่อเอาเมาส์ไปวาง · แถบเป็นภาพล้วนจึงกั้น `aria-hidden` แล้วใส่รายชื่อ `.sr-only` ไว้แทน (Docker ไม่ได้ถูกพูดถึงในการ์ดข้างล่าง) · **พร้อมกันนั้นแก้คำอธิบายสัญญาอนุญาตใน sprite** จาก `thesvg.dev (CC0-1.0)` เป็น `thesvg.org (MIT)` — โดเมนเดิม redirect มาที่นี่ และไฟล์ LICENSE ของ repo คือ MIT |
| **ISSUE-35** | ✅ **ทำแล้ว** (เจ้าของสั่ง) | `index.html` sprite + `#brings` · `style.css` · `script.js` | เปลี่ยนการ์ดทักษะ 4 ใบ (หัวข้อ + รายการข้อความ) เป็น **กริดไทล์โลโก้ 32 ใบ แบ่ง 4 หมวด** ตามภาพอ้างอิงที่เจ้าของส่งมา · เพิ่ม `<symbol>` อีก 21 อัน (โลโก้ 21 ตัวจาก thesvg.org · MIT) รวมทั้งไฟล์เป็น 27 โลโก้ + 10 ไอคอน ทุกอันถูกใช้จริง ไม่มีตัวลอย · **5 อย่างที่ไม่มีโลโก้แบรนด์ให้ใช้** (Visio, Netpie, Wokwi ไม่มีในคลัง · Wireframing, Prototype เป็นทักษะไม่ใช่โปรแกรม) วาดเป็น **ไอคอนเส้น 24×24 ใช้ `currentColor`** สีเดียวกับตัวหนังสือ ตั้งใจให้หน้าตาต่างจากโลโก้สีของจริงชัด ๆ จะได้ไม่หลอกว่าเป็นโลโก้ทางการ · **เจอบั๊กระหว่างทาง 4 ข้อ:** **(1) MySQL หายไปทั้งใบ** — `default.svg` ของ MySQL เป็นภาพ**สีขาวล้วน** (`fill="#FFF"` ทำมาสำหรับพื้นมืด) มองไม่เห็นบนไทล์ขาว แก้โดยเปลี่ยนเป็นสีน้ำเงินแบรนด์ `#00758F` (contrast บนพื้นขาว 5.33:1) · ลอง `mono.svg` ก่อนแล้วแต่มีตัวหนังสือ "MySQL" ติดมาในภาพ ซ้ำกับป้ายชื่อใต้โลโก้ จึงกลับมาใช้รูปโลมาอย่างเดียว · **PHP เจอปัญหาเดียวกัน** เปลี่ยนเป็น `#777BB4` (3.98:1) · **(2) ป้ายชื่อไม่ตรงแนว** — โลโก้ทรงสูง (Java, Figma, Node.js) ดันป้ายชื่อลงมาต่ำกว่าใบอื่น แก้โดยล็อกกล่องเป็น**จัตุรัส 44×44** ให้ภาพย่อพอดีเองตาม viewBox แล้วชดเชยขนาดที่ตาเห็นด้วย `transform: scale()` ซึ่งไม่กระทบขนาดกล่อง (PHP เป็นโลโก้แนวนอน 100×50 พอลงกล่องจัตุรัสจะสูงแค่ครึ่งเดียว ต้อง scale 1.35) · **(3) `@media` เรียงผิดลำดับ** — บล็อก `max-width: 600px` ของกริดถูกวางไว้**ก่อน** `max-width: 768px` พอจอแคบกว่า 600 ทั้งคู่เข้าเงื่อนไขและ specificity เท่ากัน **ตัวที่อยู่ทีหลังในไฟล์ชนะ** กฎมือถือจึงไม่เคยทำงานเลย (อาการที่เห็นคือจอ 594px กลับได้ไทล์ใหญ่กว่าจอ 674px) — ย้ายบล็อก 600 ไปไว้หลัง 768 · **กฎที่ต้องจำ: `max-width` ต้องเรียงจากกว้างไปแคบเสมอ** ไล่เช็คทั้งไฟล์แล้วคู่อื่นเรียงถูกอยู่ · **(4) ขนาดไทล์กระโดด** — จาก 161px ที่จอ 754 ไปเหลือ 98px ทันทีที่ต่ำกว่า 600 เพิ่มขั้นกลางใน `@media 768` ที่มีอยู่แล้ว (ไม่เพิ่ม breakpoint ใหม่) วัดแล้วช่วงขนาดไทล์อยู่ที่ 98–171px ไม่ล้นแนวนอนทุกความกว้างที่ทดสอบ (1414 / 974 / 794 / 728 / 674 / 594 / 496) · **เก็บกวาดตามมา:** ลบ CSS ที่ตายแล้ว 20 rule / 132 บรรทัด (`.brings-item`, `.brings-visual-grid` และ `@media` ที่เหลือแต่บล็อกเปล่า) — ระวังว่า `.brings-icon` **ห้ามลบ** เพราะใบรับรองกับช่องทางติดต่อยังใช้อยู่ · ลบบล็อก ripple ของ `.brings-item` ใน `script.js` (ซ้ำกับของ `.clickable-image` ทั้งดุ้น) · เติม **Docker** เข้าหมวด Database & Backend เพราะมันโผล่ในแถบ Logo Loop แต่ไม่เคยถูกพูดถึงในรายการทักษะเลย · รันตรวจ CSS ตายอีกรอบได้ **0 rule** · **ผลข้างเคียงที่ต้องรู้:** `index.html` โตเป็น **97 KB** เพราะ sprite ฝังอยู่ใน HTML (gzip แล้วเหลือ **27 KB**) ยอมรับได้เพราะแลกกับการไม่ต้องยิง request เพิ่มเลยสักอัน |
| **ISSUE-36** | ⬜ **ยังไม่ทำ** (ข้อเสนอ · รอเจ้าของตัดสินใจ) | `index.html:25` · Certificates | หลัง ISSUE-35 เอาไอคอน Font Awesome ออกไป 4 จุด **ตอนนี้ทั้งเว็บเหลือใช้ Font Awesome แค่ไอคอนเดียวคือ `fa-certificate` ใน 6 จุด** แต่ยังโหลด stylesheet ทั้งก้อนจาก cdnjs อยู่ ซึ่งเป็น **render-blocking request จากโดเมนภายนอก** · ถ้าย้ายไปเป็น `<symbol id="icon-cert">` ใน sprite เหมือนไอคอนอื่นจะ **ตัด `<link>` ไป CDN ออกได้ทั้งบรรทัด** หน้าโหลดเร็วขึ้นและไม่พึ่งบริการภายนอกเพิ่มอีกหนึ่งเจ้า · งานประมาณ 10 นาที |
| **ISSUE-38** | ✅ **แก้แล้ว** (เจ้าของแจ้งว่าดูไม่สมส่วน) | `index.html` `#workshop` · `style.css:1327-1375` | **ส่วน Workshop เหลือช่องว่างครึ่งจอ** — เนื้อหาถูกใส่ไว้ใน `.projects-grid` ซึ่งเป็น `grid-template-columns: repeat(2, 1fr)` (ตั้งไว้สำหรับส่วน Projects ที่มีหลายการ์ด) แต่ Workshop มีการ์ดใบเดียว คอลัมน์ขวาจึงว่างเปล่า 500px · **แก้:** เลิกใช้การ์ดครอบ เปลี่ยนเป็น `.doc-grid` ของตัวเอง แล้วแตกเอกสารเป็น **การ์ดใบละเรื่อง 4 ใบ 2 คอลัมน์** เต็มความกว้าง 1020px · **พบว่าเนื้อหาถูกขายต่ำกว่าความจริงมาก** — อ่านไฟล์ PDF จริงด้วย pdf.js พบว่าเป็นรายงานวิเคราะห์ระบบ **91 / 86 / 74 หน้า** แต่หน้าเว็บเขียนแค่ `PDF · 2567` · เปลี่ยน `.doc-meta` เป็น `จำนวนหน้า · PDF · ปี` · **และ `Cheese.pdf` ไม่ใช่รายงาน** — เป็นสไลด์นำเสนอ 22 หน้า ขนาด 1440x810pt (16:9) ถูกจัดปนกับรายงาน A4 อยู่ เพิ่มป้าย `.doc-kind` แยกชนิด (รายงานวิเคราะห์ระบบ / สไลด์นำเสนอ) พร้อม `<symbol id="icon-slides">` ใหม่ · **เก็บรายละเอียดที่ค้างมานาน:** ปุ่มเดิมเขียนว่า "ดู" สองตัวอักษร พอเจอ `.btn-sm` เลยกลายเป็นจุดกลม ๆ ดูไม่สมกับปุ่ม "ดาวน์โหลด" ข้าง ๆ เปลี่ยนเป็น "ดูเอกสาร" · CSS เดิมใช้สีดิบ `#999` กับ `rgba(0,0,0,0.07)` ซึ่งเป็นเทากลาง หลุดจากพาเลตอมฟ้าของเว็บ เปลี่ยนมาใช้ token `--muted` / `--border` · แถวปุ่มดันชิดล่างด้วย `margin-top: auto` การ์ดที่ชื่อยาวสองบรรทัดปุ่มจึงยังตรงแนวกับใบอื่น · **วัดแล้ว** 1280/900/700/560/420px: ไม่ล้นแนวนอน ปุ่มไม่ตกบรรทัดสักใบ การ์ดในแถวเดียวกันสูงเท่ากันทุกใบ · CSS ตาย 0 rule |

---

> **หมายเหตุ:** เอกสารนี้อธิบาย**สภาพจริงของโค้ด ณ วันที่ 4 ก.ย. 2026** ไม่ใช่สเปกในอุดมคติ
> เมื่อแก้ปัญหาใน §14 แล้ว ให้อัปเดตตารางนี้ด้วย
